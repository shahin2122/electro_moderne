export interface IContactUsRequest{
    id: number;
    title: string;
    email: string;
    submitedDate: string;
    fullName: string;
    phoneNumber: string;
    context: string;
    status: string;
}